# 📁 ساختار پروژه

```
family-tree-system/
│
├── 📄 README.md                          # مستندات کامل پروژه
├── 📄 QUICKSTART.md                      # راهنمای سریع شروع
├── 📄 .gitignore                         # فایل‌های نادیده گرفته شده Git
├── 🔧 install.sh                         # اسکریپت نصب خودکار
├── 🚀 run.sh                             # اسکریپت اجرای سریع
├── 🧪 test_family_tree.py                # تست‌های سیستم
├── 📊 sample_family.json                 # داده نمونه JSON
│
├── 🐍 backend/                           # بک‌اند Python
│   ├── 🎯 app.py                         # سرور Flask (REST API)
│   ├── 🌳 family_tree.py                 # لاجیک اصلی درخت خانوادگی
│   └── 📦 requirements.txt               # وابستگی‌های Python
│
└── ⚛️ frontend/                          # فرانت‌اند React
    ├── 📄 index.html                     # HTML اصلی
    ├── 📄 package.json                   # وابستگی‌های Node.js
    ├── 🔧 vite.config.js                 # تنظیمات Vite
    │
    └── 📂 src/                           # سورس کد React
        ├── 🎯 main.jsx                   # نقطه ورود
        ├── 🎨 App.jsx                    # کامپوننت اصلی
        ├── 💅 App.css                    # استایل اصلی
        ├── 💅 index.css                  # استایل گلوبال
        │
        └── 📂 components/                # کامپوننت‌های React
            ├── 👤 PersonManager.jsx       # مدیریت افراد
            ├── 💅 PersonManager.css
            │
            ├── 🔗 RelationshipManager.jsx # مدیریت روابط
            ├── 💅 RelationshipManager.css
            │
            ├── 🔍 PathFinder.jsx          # جستجوی مسیر و نسبت
            ├── 💅 PathFinder.css
            │
            ├── 🌳 FamilyTreeVisualization.jsx  # نمایش درخت
            └── 💅 FamilyTreeVisualization.css
```

## 📊 نمودار جریان داده

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React)                         │
│                      http://localhost:3000                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Person     │  │ Relationship │  │  PathFinder  │          │
│  │   Manager    │  │   Manager    │  │              │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                 │                  │                  │
│         └─────────────────┼──────────────────┘                  │
│                           │                                     │
│                  ┌────────▼────────┐                            │
│                  │   FamilyTree    │                            │
│                  │  Visualization  │                            │
│                  │  (React Flow)   │                            │
│                  └─────────────────┘                            │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          │ HTTP/REST API
                          │ JSON
                          │
┌─────────────────────────▼───────────────────────────────────────┐
│                     BACKEND (Python/Flask)                       │
│                      http://localhost:5000                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │                     app.py (Flask)                      │    │
│  │                      REST API Server                    │    │
│  └────────────────────────┬───────────────────────────────┘    │
│                           │                                     │
│  ┌────────────────────────▼───────────────────────────────┐    │
│  │                 family_tree.py                          │    │
│  │  ┌──────────────────────────────────────────────┐      │    │
│  │  │           FamilyTree Class                    │      │    │
│  │  │  • افزودن/حذف افراد                         │      │    │
│  │  │  • مدیریت روابط                              │      │    │
│  │  │  • BFS Algorithm                             │      │    │
│  │  │  • DFS Algorithm                             │      │    │
│  │  │  • Persian Relationship Simplifier           │      │    │
│  │  └──────────────────────────────────────────────┘      │    │
│  └──────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 جریان کار (Workflow)

### 1. افزودن فرد جدید
```
User → PersonManager → POST /api/person → FamilyTree.add_person() → Database Update → Refresh UI
```

### 2. افزودن رابطه
```
User → RelationshipManager → POST /api/relationship/{type} → FamilyTree.add_relationship() → Update Graph → Refresh UI
```

### 3. جستجوی نسبت
```
User → PathFinder → POST /api/path/{algorithm} 
                  ↓
            FamilyTree.{bfs|dfs}_find_path()
                  ↓
            simplify_relationship()
                  ↓
            Return Path + Persian Relationship
                  ↓
            Display Results
```

## 🎯 کامپوننت‌های کلیدی

### Backend (Python)

#### `FamilyTree` Class
- `add_person()`: افزودن فرد جدید
- `remove_person()`: حذف فرد
- `add_parent_child()`: افزودن رابطه والد-فرزند
- `add_spouse()`: افزودن رابطه همسری
- `bfs_find_path()`: جستجوی BFS
- `dfs_find_path()`: جستجوی DFS
- `simplify_relationship()`: تبدیل به نسبت فارسی

#### Flask API Endpoints
- `GET /api/people`: دریافت لیست افراد
- `POST /api/person`: افزودن فرد
- `DELETE /api/person/{id}`: حذف فرد
- `POST /api/relationship/parent-child`: افزودن رابطه والد-فرزند
- `POST /api/relationship/spouse`: افزودن رابطه همسری
- `POST /api/path/bfs`: جستجوی BFS
- `POST /api/path/dfs`: جستجوی DFS
- `POST /api/path/compare`: مقایسه الگوریتم‌ها

### Frontend (React)

#### `App.jsx`
- مدیریت state اصلی
- ارتباط با API
- نمایش notification

#### `PersonManager.jsx`
- فرم افزودن فرد
- لیست افراد موجود
- حذف افراد

#### `RelationshipManager.jsx`
- انتخاب نوع رابطه
- فرم افزودن رابطه والد-فرزند
- فرم افزودن رابطه همسری

#### `PathFinder.jsx`
- انتخاب دو فرد
- انتخاب الگوریتم
- نمایش نتایج جستجو
- مقایسه BFS و DFS

#### `FamilyTreeVisualization.jsx`
- نمایش گرافیکی درخت با React Flow
- گروه‌بندی نسل‌ها
- رنگ‌بندی بر اساس جنسیت
- Zoom و Pan

## 🛠️ تکنولوژی‌های استفاده شده

### Backend Stack
- Python 3.8+
- Flask 3.0 (Web Framework)
- Flask-CORS (CORS Support)

### Frontend Stack
- React 18.2 (UI Library)
- Vite 5.0 (Build Tool)
- React Flow 11.10 (Graph Visualization)

### Algorithms
- BFS (Breadth-First Search)
- DFS (Depth-First Search)
- Graph Traversal
- Path Finding
- Relationship Mapping

## 🔐 امنیت و بهینه‌سازی

- ✅ Validation ورودی‌ها
- ✅ Error Handling جامع
- ✅ CORS Configuration
- ✅ Responsive Design
- ✅ RTL Support (راست به چپ)
- ✅ Persian Font Support
