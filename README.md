# 🌳 سیستم شجره‌نامه خانوادگی

یک سیستم کامل و پیشرفته برای مدیریت و نمایش شجره‌نامه خانوادگی با استفاده از الگوریتم‌های DFS و BFS

## ✨ امکانات

### 🎯 امکانات اصلی
- ✅ افزودن و حذف افراد با اطلاعات کامل (نام، جنسیت، سال تولد)
- 🔗 مدیریت روابط خانوادگی (والد-فرزند، همسر)
- 🔍 جستجوی مسیر بین دو فرد با دو الگوریتم:
  - **BFS (Breadth-First Search)**: جستجوی سطح به سطح
  - **DFS (Depth-First Search)**: جستجوی عمقی
- 🧠 تبدیل هوشمند مسیرها به نسبت‌های فارسی
- 📊 نمایش بصری زیبا با React Flow
- 🎨 رابط کاربری مدرن و کاربرپسند

### 📝 نسبت‌های فارسی پشتیبانی شده
سیستم می‌تواند نسبت‌های پیچیده را به زبان ساده فارسی تبدیل کند:

#### نسبت‌های مستقیم
- پدر، مادر
- پسر، دختر
- شوهر، همسر
- برادر، خواهر

#### نسبت‌های غیرمستقیم
- پدربزرگ، مادربزرگ (پدری/مادری)
- نوه پسر، نوه دختر
- عمو، عمه (فرزندان پدربزرگ پدری)
- دایی، خاله (فرزندان مادربزرگ مادری)
- پسر عمو، دختر عمه
- پسر دایی، دختر خاله
- برادر شوهر، خواهر زن
- پسر برادر، دختر خواهر
- عروس، داماد
- و بسیاری دیگر...

## 🏗️ معماری پروژه

```
family-tree/
├── backend/                    # بک‌اند Python با Flask
│   ├── app.py                 # سرور REST API
│   ├── family_tree.py         # منطق اصلی درخت خانوادگی
│   └── requirements.txt       # وابستگی‌های Python
│
└── frontend/                   # فرانت‌اند React
    ├── src/
    │   ├── components/        # کامپوننت‌های React
    │   │   ├── PersonManager.jsx
    │   │   ├── RelationshipManager.jsx
    │   │   ├── PathFinder.jsx
    │   │   └── FamilyTreeVisualization.jsx
    │   ├── App.jsx           # کامپوننت اصلی
    │   └── main.jsx          # نقطه ورود
    ├── package.json          # وابستگی‌های Node.js
    └── index.html            # HTML اصلی
```

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها
- Python 3.8 یا بالاتر
- Node.js 16 یا بالاتر
- npm یا yarn

### 1️⃣ راه‌اندازی بک‌اند

```bash
# رفتن به پوشه بک‌اند
cd backend

# نصب وابستگی‌ها
pip install -r requirements.txt

# اجرای سرور
python app.py
```

سرور روی `http://localhost:5000` اجرا می‌شود.

### 2️⃣ راه‌اندازی فرانت‌اند

```bash
# رفتن به پوشه فرانت‌اند
cd frontend

# نصب وابستگی‌ها
npm install

# اجرای سرور توسعه
npm run dev
```

رابط کاربری روی `http://localhost:3000` در دسترس است.

## 📚 راهنمای استفاده

### مدیریت افراد
1. در بخش "👤 مدیریت افراد" روی فرم را پر کنید
2. شناسه یکتا برای فرد وارد کنید (مثال: p1, p2, ...)
3. نام، جنسیت و سال تولد را وارد کنید
4. روی "➕ افزودن فرد" کلیک کنید

### مدیریت روابط
1. در بخش "🔗 مدیریت روابط" نوع رابطه را انتخاب کنید
2. برای رابطه والد-فرزند: والد و فرزند را انتخاب کنید
3. برای رابطه همسری: هر دو همسر را انتخاب کنید
4. روی دکمه افزودن کلیک کنید

### جستجوی نسبت خانوادگی
1. در بخش "🔍 یافتن نسبت خانوادگی" دو فرد را انتخاب کنید
2. الگوریتم مورد نظر را انتخاب کنید:
   - **مقایسه BFS و DFS**: هر دو الگوریتم را اجرا و مقایسه می‌کند
   - **فقط BFS**: کوتاه‌ترین مسیر را پیدا می‌کند
   - **فقط DFS**: اولین مسیر پیدا شده را نمایش می‌دهد
3. روی "🔍 جستجوی نسبت" کلیک کنید

## 🔧 API Documentation

### افزودن فرد
```http
POST /api/person
Content-Type: application/json

{
  "id": "p1",
  "name": "علی",
  "gender": "male",
  "birth_year": 1370
}
```

### حذف فرد
```http
DELETE /api/person/{person_id}
```

### افزودن رابطه والد-فرزند
```http
POST /api/relationship/parent-child
Content-Type: application/json

{
  "parent_id": "p1",
  "child_id": "p2"
}
```

### افزودن رابطه همسری
```http
POST /api/relationship/spouse
Content-Type: application/json

{
  "person1_id": "p1",
  "person2_id": "p2"
}
```

### جستجوی مسیر با BFS
```http
POST /api/path/bfs
Content-Type: application/json

{
  "start_id": "p1",
  "end_id": "p5"
}
```

### جستجوی مسیر با DFS
```http
POST /api/path/dfs
Content-Type: application/json

{
  "start_id": "p1",
  "end_id": "p5"
}
```

### مقایسه هر دو الگوریتم
```http
POST /api/path/compare
Content-Type: application/json

{
  "start_id": "p1",
  "end_id": "p5"
}
```

## 🎨 تکنولوژی‌های استفاده شده

### بک‌اند
- **Python 3**: زبان اصلی
- **Flask**: فریمورک وب
- **Flask-CORS**: مدیریت CORS

### فرانت‌اند
- **React 18**: کتابخانه UI
- **React Flow**: نمایش بصری درخت
- **Vite**: ابزار ساخت
- **CSS3**: استایل‌دهی مدرن

### الگوریتم‌ها
- **BFS (Breadth-First Search)**: برای پیدا کردن کوتاه‌ترین مسیر
- **DFS (Depth-First Search)**: برای جستجوی عمقی
- **الگوریتم تبدیل نسبت**: تبدیل مسیرها به فارسی

## 🌟 ویژگی‌های پیشرفته

### الگوریتم تبدیل به نسبت فارسی
سیستم از یک الگوریتم پیچیده برای تبدیل مسیرها استفاده می‌کند:

1. **تحلیل مسیر**: بررسی نوع و تعداد گام‌ها
2. **شناسایی الگو**: تشخیص الگوهای متداول (مثل والد→والد→فرزند = عمو/دایی)
3. **تبدیل به فارسی**: تبدیل به نسبت فارسی مناسب با توجه به جنسیت

### نمایش بصری هوشمند
- **گروه‌بندی نسل‌ها**: افراد به صورت خودکار در نسل‌های مختلف قرار می‌گیرند
- **رنگ‌بندی**: مردان آبی، زنان قرمز
- **انیمیشن روابط**: روابط همسری با خط متحرک نمایش داده می‌شود
- **Zoom و Pan**: امکان بزرگنمایی و حرکت در درخت

## 🔮 توسعه‌های آینده

- [ ] ذخیره‌سازی در پایگاه داده
- [ ] احراز هویت کاربران
- [ ] آپلود و دانلود فایل
- [ ] صدور گزارش PDF
- [ ] اپلیکیشن موبایل
- [ ] نسبت‌های پیچیده‌تر
- [ ] جستجوی پیشرفته

## 👨‍💻 توسعه‌دهنده

این پروژه توسط Claude (Anthropic) با کمک هوش مصنوعی توسعه یافته است.

## 📄 مجوز

این پروژه تحت مجوز MIT منتشر شده است.

## 🙏 تشکر

از تمام افرادی که در توسعه این پروژه مشارکت کرده‌اند، تشکر می‌کنیم!

---

**ساخته شده با ❤️ در ایران**
